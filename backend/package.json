{
  "name": "perseo-backend",
  "version": "2.0.0",
  "description": "Perseo SaaS — Backend API",
  "main": "src/index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon src/index.js",
    "build": "cd ../frontend && npm install --include=dev && npm run build && mkdir -p ../backend/.next && cp -r .next/standalone ../backend/.next/ && cp -r .next/static ../backend/.next/standalone/.next/"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "googleapis": "^128.0.0",
    "jsonwebtoken": "^9.0.2",
    "node-cron": "^3.0.3",
    "openai": "^4.20.1",
    "pg": "^8.11.3",
    "stripe": "^14.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
